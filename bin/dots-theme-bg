#!/usr/bin/env bash
# dots:summary=Manage theme backgrounds
# dots:usage=[SUBCOMMAND]
DT_LIB=$(cd -- "${BASH_SOURCE[0]%/*}/../lib/dots/themes" && pwd) || exit
set -- bg "$@"
source "$DT_LIB/commands.bash"
