#!/usr/bin/env bash
# dots:summary=Reapply the remembered theme background
DT_LIB=$(cd -- "${BASH_SOURCE[0]%/*}/../lib/dots/themes" && pwd) || exit
set -- bg select "$@"
source "$DT_LIB/commands.bash"
